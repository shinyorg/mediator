<Project>
    <Import Project="$(MSBuildThisFileDirectory)Shiny.Mediator.SourceGenerators.targets" />

    <ItemGroup Condition=" '$(ImplicitUsings)' == 'true' OR '$(ImplicitUsings)' == 'enable' ">
        <Using Include="Shiny.Mediator" />
    </ItemGroup>
</Project>